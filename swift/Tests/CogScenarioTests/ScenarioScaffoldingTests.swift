import Testing

/// Placeholder so `CogScenarioTests` has selectable work before the `M5`
/// run-count assertions land.
@Test func `CogScenarioTests target has selectable work`() {
  #expect(Bool(true))
}
