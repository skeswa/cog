/// Scaffolding placeholder for the `Cog` library.
///
/// `M0-01` only establishes the package shape. The real graph, `Cogtext`, and
/// public ref types arrive in `M1`.
enum CogScaffolding {
  /// Marker the M0 sentinel tests use to prove the target links.
  static let marker = "Cog"
}
