# Fixture scenarios: a proof mode its verification never uses

- **DECL-05.** The read-only projection reads back.
- **DECL-06.** Writing through the read-only projection is rejected at compile
  time. (Proof: compile-fail.)
