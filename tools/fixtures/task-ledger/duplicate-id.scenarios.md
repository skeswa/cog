# Fixture scenarios: duplicate task ID

The census `duplicate-id.md` owns. Both entries that share `M1-02a` green a
different scenario, so only the ID check fires.

- **DECL-01.** The first behavior.
- **DECL-02.** The second behavior.
